class Solution {
    public void rotate(int[] nums, int k) {
        for(int x=1; x<=k;x++){
            int temp = nums[0], n=nums.length;
            for(int i=0; i<n-1; i++)
                nums[i]=nums[i+1];
            nums[n-1] = temp;
        }
        
    
    }
}
